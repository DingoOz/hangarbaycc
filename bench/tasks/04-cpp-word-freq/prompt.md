Write a complete C++ program (C++17) that reads whitespace-separated words
from stdin until EOF and prints each unique word with its count, one per line,
in the format `word count`, sorted alphabetically by word.

Example: input `apple banana apple cherry banana apple` ->
```
apple 3
banana 2
cherry 1
```
