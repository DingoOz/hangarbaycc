Write a complete C program that reads one line of text from stdin and prints
the words in reverse order, separated by single spaces, followed by a newline.

Words in the input are separated by single spaces. Example:
input `the quick brown fox` -> output `fox brown quick the`
